# 部署说明（目标机器使用）

## 目录结构（整个文件夹拷贝到目标机任意位置即可，无需安装 Python）

```
发布目录/
├── 报告生成工具.exe     ← 主程序（内含 Python 3.13 运行时 + 全部标准库 + 机器码核心）
├── _internal/           ← 运行时依赖（python313.dll + python3.dll + 机器码 pyd，勿删）
├── libs/                ← 外部依赖（pandas/numpy/cv2/openpyxl/PIL + six + Lib/DLLs 兜底）
├── PaddleOCR-json/      ← OCR 引擎（exe + 模型）
├── license.lic          ← 授权文件（每台机器单独生成）
└── 运行日志.log         ← 程序运行日志（自动生成，含全部报错）
```

## 目标机要求

- **不需要安装 Python**（运行时已捆绑在 exe 内）
- Windows 10/11 64 位
- 首次运行请将本机机器码发给软件提供方，获取 license.lic

## 运行日志（排错第一入口）

程序每次启动都会在 exe 同目录生成/追加「运行日志.log」，格式：

```
17:02:00.288 验证授权...
17:02:00.289 授权通过
17:02:00.289 启动主界面...
```

**所有报错都会自动写入该日志**（含完整 traceback）：
- 启动失败（模块导入错误）
- 授权失败（机器码不匹配 / 文件缺失 / 已过期）
- OCR 引擎 / cv2 加载失败
- 任何未捕获异常（主线程、后台线程）

## 排错

| 现象 | 处理 |
|---|---|
| 启动失败 / 无窗口 | 打开「运行日志.log」看最后几行的报错 |
| 日志停在"验证授权..." | 授权弹窗在等待：检查 license.lic 是否已拷贝、机器码是否匹配（见下） |
| 提示授权失败 | 把弹窗或日志中的机器码发给提供方换新 license.lic |
| OCR 识别失败 | 确认 PaddleOCR-json 目录完整（exe + models） |

## 授权机器码说明

- 程序实际使用的机器码 = **主板序列号**（如 `L1HF09E03A3`），无前缀
- 生成 license 时机器码必须与日志中"机器码:"一致（如 `L1HF09E03A3`）
- 注意：某些第三方工具显示的机器码带前缀（如 `e0d4e883bd95:L1HF09E03A3`），
  这种**不能直接用于生成授权**，否则会授权失败

## 工作目录（业务数据）

程序运行所需的数据目录在 GUI 中可自定义（默认 D:\baogao\sferzaibo），
包含 ping / pictuer / jietu / dt / result.xlsx / finish.xlsx / 室分模板.xlsx。
